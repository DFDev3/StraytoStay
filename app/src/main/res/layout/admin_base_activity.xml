<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    tools:context=".Main.Shelter.BaseEntity">

    <!-- 🔹 Header fixed at top -->
    <include
        android:id="@+id/header"
        layout="@layout/comp_header" />

    <!-- 🔹 Fragment container in the middle, fills remaining space -->
    <FrameLayout
        android:id="@+id/fragment_container_admin"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- 🔹 Fixed custom bottom navbar -->
    <include
        android:id="@+id/bottomNavbarEntidad"
        layout="@layout/comp_navbar_shelter"/>

</LinearLayout>
